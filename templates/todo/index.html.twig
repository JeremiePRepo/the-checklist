{% extends 'todo/layout.html.twig' %}

{% block title %}Hello TodoController!{% endblock %}

{% block body %}
<style>
    .example-wrapper {
        margin: 1em auto;
        max-width: 800px;
        width: 95%;
        font: 18px/1.5 sans-serif;
    }

    .example-wrapper code {
        background: #F5F5F5;
        padding: 2px 6px;
    }
</style>

<div class="wrapper">
    <h1>Hello</h1>
    <ul>
        {% for task in tasks %}
        <li>
            {{ task.title }}
            <a href="{{ path('todo_delete', {'id' : task.id}) }}">[X]</a>
            <a href="{{ path('todo_edit', {'id' : task.id}) }}">[Éditer]</a>
        </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}